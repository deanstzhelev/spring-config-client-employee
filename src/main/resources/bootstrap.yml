spring :
  application:
    name: employee-search
  cloud:
    config:
      uri: http://192.168.99.100:9090/

server:
  port: 8081

management:
  endpoints:
    web:
      exposure:
        include: '*'